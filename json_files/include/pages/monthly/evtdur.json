{
    "title": "Events per Duration Range",
    "id": "evtdur",
    "objects": [
        {
            "include": "sqlChart",
            "title": "Events per Duration Range",
            "sql": "select durationRange as 'Duration Range',count(*) as 'Number of Events' from attacks group by durationRange order by durationRange"
        },
        {
            "include": "lastTable"
        }
    ]
}